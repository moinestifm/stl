

```bash
git clone https://github.com/moinesti-fm/stl.git
cd stl
```
Update configs/default.yaml or create your own config file for your streams and soundcards

```bash
docker-compose up -d --build
```